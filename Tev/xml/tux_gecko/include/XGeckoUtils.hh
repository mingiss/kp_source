#ifndef XGECKO_UTILS_HH
#define XGECKO_UTILS_HH

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#include <XTux.hh>
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
#define XG_ADDREF(object)     xg_addRef(object)
////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

template <typename T>
  inline T *xg_addRef(T *object)
{
  object->AddRef();
  return object;
}

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

#endif /* XGECKO_UTILS_HH */
